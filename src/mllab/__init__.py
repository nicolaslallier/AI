"""ML & LLM utilities.

Small, typed building blocks for data processing (pandas), ML
training/evaluation (scikit-learn) and LLM inference (OpenAI-compatible
endpoints, e.g. Ollama).

The package is importable as ``mllab`` (src layout, installed editable by uv).
"""

__version__ = "0.1.0"
